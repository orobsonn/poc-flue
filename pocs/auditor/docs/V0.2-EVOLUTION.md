# auditor POC v0.2 — Evolution

Doc gerado após smoke test do v0.1. Captura o que ficou pendente, em qual ordem evoluir, e o que conscientemente fica fora de escopo.

## Contexto: o que o v0.1 validou

Smoke local com `gpt-oss-120b` rodou a pipeline ponta-a-ponta sem erro estrutural:

- ✅ Gerador sintético insere decisions no D1 com defesa PII 4 camadas
- ✅ Monitor lê candidates, faz bucketing cartesiano (3×3×2), aplica critérios SQL
- ✅ Skills carregadas via R2-mounted sandbox (`getVirtualSandbox` rejeitado por colidir com nosso keyspace; rolamos InMemoryFs custom)
- ✅ Sessions Flue paralelas (uma por chamada de skill, evita colisão "Session already running")
- ✅ Workers AI via AI Gateway, output structured válido contra schemas valibot
- ✅ Artefatos do run no R2: `analysis.md` + `proposal.md` + `divergencias.json`
- ✅ Severidade computada com SQL deltas explícitos

**O que não disparou no smoke:**
- ❌ `divergencias.json` veio vazio (`[]`) — não é bug, é limitação de design discutida abaixo
- ❌ PR + Telegram alert (consequência: sem divergência, severidade fica `info`)

## Problema central: skill auditora não recebe input do lead

A skill `detect-divergences` recebe hoje:
```ts
{
  decision: { id, did, reasoned, out_of_scope },
  gabarito: <markdown H1-H5>
}
```

O gabarito tem regras tipo "**Se o lead tem fundador técnico**, então H1 → priorizar". Pra determinar se H1 foi ignorado, o LLM precisa saber se o lead **tinha** fundador técnico. Esse dado está em `fixtures/leads.json` no momento da geração, mas evapora antes do insert no `decision_log` — só o output da decisão é persistido.

Sem o input, a LLM legitimamente retorna `divergences: []`: "não vejo evidência de heurístico aplicável que tenha sido ignorado".

## Mudança 1 — Tabela `lead` + FK em `decision_log`

### Schema

```sql
-- nova tabela
CREATE TABLE lead (
  id TEXT PRIMARY KEY,
  segmento TEXT,
  faturamento_band TEXT,          -- '0-50k' | '50k-500k' | '500k-5M' | '>5M'
  time_vendas TEXT,                -- 'dedicado' | 'solo' | null
  ferramentas TEXT,                -- 'crm' | 'planilha' | null
  sinal TEXT,                      -- 'demo' | 'form' | 'material' | null
  fundador_tecnico INTEGER,        -- 0 | 1 (derivado do contexto_livre)
  menciona_dor INTEGER,            -- 0 | 1 (derivado do contexto_livre)
  contexto_livre_sanitized TEXT    -- texto livre pós sanitizePII (existe pra H2)
);

-- FK no log
ALTER TABLE decision_log ADD COLUMN lead_id TEXT REFERENCES lead(id);
CREATE INDEX idx_decision_log_lead ON decision_log(lead_id);
```

### Por que `contexto_livre_sanitized` existe

Apenas H2 do gabarito atual depende de texto literal:
```
H2 — Dor específica em hipótese não validada
Se o lead menciona dor X que coincide com hipótese não validada do produto,
→ priorizar independente de tamanho
```

Booleano `menciona_dor: 1` só responde "menciona qualquer dor?". A skill precisa cruzar a dor específica com `H-NV1`/`H-NV2` do `contexto-momento.md`. Sem o texto, H2 vira incheckável. Sanitização (`sanitizePII`) roda no insert pra remover PII residual (telefones, emails, valores BRL).

H1, H3, H4, H5 funcionam só com os booleans.

### Mudanças no código

1. **Migration**: `migrations/0002_lead.sql` com o schema acima.
2. **Generator** (`src/lib/synthetic-generator.ts`):
   - `INSERT OR IGNORE INTO lead (...)` antes de gerar a decision (`fundador_tecnico` e `menciona_dor` derivados do regex já existente em `applyRubrica`; `contexto_livre_sanitized = sanitizePII(lead.contexto_livre)`).
   - Decision recebe `lead_id` no INSERT.
3. **Monitor** (`.flue/agents/monitor.ts`):
   - Query de candidates passa a fazer `JOIN lead ON decision_log.lead_id = lead.id`.
   - Cada `candidate` carrega o objeto `lead` aninhado.
4. **Skill** (`.flue/skills/detect-divergences/SKILL.md`):
   - Descreve novo input shape: `{ decision, lead, gabarito }`.
   - Lógica: "pra cada heurístico, verifique se as condições aplicam **olhando `lead`**; se aplicam, o `reasoned` invocou o heurístico?".
5. **Schema valibot** (`src/schemas/skills.ts`):
   - Adicionar `LeadSchema` exportada, usar no input da skill (validação de borda).

Esforço: ~80 linhas de código + 1 migration + 1 ajuste de skill description. Sem refactor estrutural.

### Limitação assumida explicitamente

POC v0.2 **assume leads imutáveis na janela de auditoria**. Se um lead é atualizado entre a decisão e a auditoria (segmento muda, sinal novo), o monitor vai analisar com dado corrente, não com snapshot histórico.

Em produção real isso quebra: `lead.segmento` em t=0 (decisão) vs t=1 (audit) podem divergir; auditor avalia decisão com input errado. Solução de produção seria `lead_snapshot` imutável + `lead_version_id` no log (padrão de sistemas de compliance). Fora do escopo desta prova de conceito porque o gerador sintético nunca muta leads — a falha não se manifesta.

## Mudança 2 — Modos de drift que exercitam contradição tier↔julg

Os 3 modos drift atuais (`drift-h1`, `drift-multi`) fazem o gerador ignorar heurísticos, mas mantêm **alinhamento tier↔julg** (decisão segue o tier objetivo quando o heurístico não dispara). Resultado: zero decisões em `descartar/A` ou `priorizar/C`, então o critério SQL #3 (regression / contradição obj↔julg) nunca firra no smoke.

### Adicionar modo `drift-flip`

```ts
'drift-flip': {
  ignore_h1_probability: 0,
  ignore_h2_probability: 0,
  out_of_scope_probability: 0.10,
  flip_against_tier_probability: 0.20,  // novo
  cost_multiplier: 1,
  duration_multiplier: 1,
}
```

`simulateDecision` consulta `flip_against_tier_probability`: se rolar, inverte a decisão contra o tier (ex: tier A → `descartar`; tier C → `priorizar`). Reasoned: `"descartar mesmo tier A — interlocutor sinalizou descomprometimento na call"` (genérico, sem citar heurístico).

Isso popula as buckets contradictórios E exercita `detectRegression` no SQL pipeline.

## Mudança 3 — Thresholds SQL calibrados

`detectOutOfScopeGrowth` requer `>= 20pp` de crescimento. No smoke o gerador produziu 12pp e o critério não firou. Pra POC, 20pp é alto — calibrar contra range esperado do gerador sintético:

```ts
// src/lib/criteria.ts
const OUT_OF_SCOPE_GROWTH_THRESHOLD_PP = 10;  // era 20
```

Mesmo raciocínio pra `REGRESSION_THRESHOLD_PCT` e `BUDGET_BLOW_THRESHOLD_PCT` — ajustar pra que `drift-multi` + `drift-flip` gerem severidade `warn` ou `critical` no smoke, sem precisar de baseline histórico de produção.

## Mudança 4 — Baseline pra critérios SQL no primeiro run

`runSqlCriteria` compara janela atual vs janela anterior do mesmo tamanho. No **primeiro** run não há janela anterior — todos os baselines vão a 0, `regression` retorna `triggered: false` mesmo em caso real, `budget_blow` idem.

Mitigação simples: gerador roda 2× antes do primeiro monitor (1× pra popular baseline, 1× pra criar window atual). Mais explícito: comando `npm run seed-baseline` que faz isso. Documentar em README.

## Mudança 5 — Skill recebe `contexto_momento` também

`classify-origin` decide entre 4 targets, sendo 2 (`gabarito-stale` e `contexto-mudou`) que dependem de **comparar reasoned com contexto-momento.md**. Hoje a skill só recebe a divergência + gabarito — não o contexto-momento. Sintoma: classificações cegas para esses 2 targets.

Fix: monitor lê `contexto-momento.md` do R2 (mesmo padrão de `gabarito`) e passa nos `args` de `classify-origin` e `suggest-adjustment`. ~5 linhas no monitor.

## Out of scope (v0.2 não faz)

- **`lead_snapshot` imutável + versionamento**. Concerns de produção; gerador sintético não exercita lead mutation.
- **PII real**. Gerador trabalha com `fixtures/leads.json` curado. `defendPII` segue protegendo contra falsos positivos do `applyRubrica`/heurísticos, mas não exercita exposição real.
- **Múltiplos agentes monitorados**. Pipeline assume `agent_id = qualificador` literal. Generalizar pra N agentes pede refator do `audit_run`, candidates query, etc.
- **Persistência de findings** (`active` → `resolved` → `stale`). Lib `src/lib/resolution.ts` existe e tem testes, mas não está plugada no pipeline do monitor v0.2. Adicionar exige tabela `finding` no D1 e fluxo de read/write em cada run. ~150 linhas.
- **Retry/backoff em chamadas LLM**. Flue tem retry built-in pro extractResult; não exercitamos failure de provider (rate limit, gateway 5xx).

## Limitação estrutural do Flue — `result: Schema` é prompt-based

O Flue v0.3.x trata `session.skill({ args, result: Schema })` como **contrato de validação post-hoc**, não constraint de geração no provider:

1. Schema é serializado em markdown e injetado no prompt: "você DEVE produzir output entre `---RESULT_START---` e `---RESULT_END---`..."
2. Resposta crua passada por `JSON.parse` + `valibot.safeParse`
3. Falha → retry N vezes com mesmo prompt

A SDK `pi-ai` (que Flue usa) **não plumba** `response_format: { type: "json_schema" }` (OpenAI/Workers AI) nem `tool_use` com `input_schema` (Anthropic) pra forçar structured output no decoder. Modelos pequenos sem reasoning (ex: Llama 4 Scout 17B) falham nesse contrato; modelos com reasoning + 100B+ aguentam.

**No v0.1 a troca pra `gpt-oss-120b` resolveu o sintoma.** Mas é trade-off custo vs robustez. Vias pra v0.2+ (todas opcionais):

| Via | Esforço | Trade-off |
|---|---|---|
| Aceitar e seguir com modelo forte | 0 | Custo de inferência ~3× vs modelo médio; aceitável pro POC |
| Wrap fora de Flue: chamar Workers AI direto com `response_format` em `src/lib/llm-structured.ts` | ~30 linhas | Bypass parcial de Flue (perde role/history pro path structured) |
| Patch local em `pi-ai` adicionando `responseFormat` no provider OpenAI-compat | ~20 linhas | Frágil a updates de SDK |
| Upstream PR no Flue/pi-ai | semanas | Solução certa, mas dependência externa |

Nenhuma dessas é blocker pro POC se mantivermos `gpt-oss-120b` como default.

## Ordem de execução proposta pra v0.2

1. Migration `lead` + FK + JOIN no monitor (**Mudança 1**)
2. Skill recebe `contexto_momento` (**Mudança 5**) — barata e desbloqueia 2 targets
3. Modo `drift-flip` + thresholds calibrados (**Mudanças 2 + 3**)
4. Comando `npm run seed-baseline` (**Mudança 4**)
5. Re-rodar smoke e confirmar:
   - `divergencias.json` com 3+ divergências reais
   - Severidade `warn` ou `critical`
   - PR criado, Telegram disparado

Estimativa de esforço total: ~250 linhas de código + 1 migration + 4 ajustes de SKILL.md. ~1 dia focado.

## Não-objetivos da v0.2

A v0.2 **não** precisa:
- Subir bar arquitetural pra produção (versionamento, multi-tenant, RBAC)
- Atingir 100% de cobertura de divergências possíveis
- Otimizar custo/latência de chamadas LLM

O objetivo é **um único smoke verde end-to-end** que demonstre: gerador → log → bucketing → divergência detectada → classificação → sugestão → PR + Telegram. Provar a tese, não escalar a implementação.
